#include<stdio.h>
#include"twj.h"
int main(int argc, char **argv)
{

	
	int count;
	float beita[N];
	stuData s[N];
	count=input(s);
	Beita(s,beita,count);
	print(beita,count);
	return 0;
}
